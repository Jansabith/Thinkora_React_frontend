# Production build: the API is on the SAME domain as the website.
# Vercel rewrites forward /thinkora-api/... to the Django backend.
# This MUST be an empty string (not undefined) to override the .env base value.
VITE_API_BASE_URL=""
VITE_API_PREFIX=/thinkora-api
